package gente.oss.betfair.channels.impl

import gente.oss.betfair.channels.impl.ChannelProtocol.Event

object ChannelEventHandler {

  def apply(): (ChannelState, Event) => ChannelState = {
    ???
  }
}
